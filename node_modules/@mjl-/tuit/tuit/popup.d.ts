import * as dom from '../dom';
import * as types from './types';
export declare enum PopupSize {
    Small = 0,
    Medium = 1,
    Large = 2
}
export declare class Popup {
    closed: Promise<boolean>;
    closedResolve: (v: boolean) => void;
    private background;
    private popup;
    constructor(app: types.Looker & dom.Rooter, view: types.UI, size?: PopupSize);
    close(): void;
    cancel(): void;
}
