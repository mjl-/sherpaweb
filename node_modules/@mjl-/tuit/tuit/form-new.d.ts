import * as dom from '../dom';
import * as types from './types';
import * as form from './form';
export declare class FormNew<Item> implements types.UI, types.Stater {
    root: HTMLElement;
    fieldValues: form.FieldValue[];
    constructor(app: dom.Rooter & types.Saver & types.Loader & types.Looker & types.StateSaver, objectName: string, fields: form.Field[], create: (object: Item) => Promise<void>);
    focus(): void;
    loadState(state: types.State): Promise<void>;
    currentState(): types.State;
}
