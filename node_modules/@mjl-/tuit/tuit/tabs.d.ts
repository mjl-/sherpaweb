import * as dom from '../dom';
import * as types from './types';
export declare class Tabs implements types.UI, types.Stater {
    private app;
    tabs: {
        label: string;
        name: string;
        ui: types.UI & types.Stater;
    }[];
    root: HTMLElement;
    selectedBox: HTMLElement;
    activeIndex: number;
    buttons: HTMLButtonElement[];
    constructor(app: dom.Rooter & types.Saver & types.Loader & types.Looker & types.StateSaver, tabs: {
        label: string;
        name: string;
        ui: types.UI & types.Stater;
    }[]);
    openTab(name: string): Promise<types.UI & types.Stater>;
    select(index: number): void;
    loadTab(state: types.State, index: number): Promise<void>;
    focus(): void;
    loadState(state: types.State): Promise<void>;
    currentState(): types.State;
}
