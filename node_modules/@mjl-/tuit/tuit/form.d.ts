import * as types from './types';
export declare type FieldKind = 'line' | 'email' | 'number' | 'multiline' | 'checkbox' | 'select' | 'multiselect' | 'radio' | 'date';
export interface Field {
    label: string;
    name: string;
    kind: FieldKind;
    required: boolean;
    readonly: boolean;
    options?: {
        label: string;
        value: any;
    }[];
}
export interface FieldValue {
    element: HTMLElement;
    value: () => any;
    focusable?: HTMLElement;
}
export declare const makeFieldValue: (app: types.Looker, field: Field, value?: any) => FieldValue;
