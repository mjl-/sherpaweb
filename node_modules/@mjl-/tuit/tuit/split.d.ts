import * as dom from '../dom';
import * as types from './types';
export declare class Split implements dom.Rooter {
    root: HTMLElement;
    constructor(app: types.Looker, ...elems: HTMLElement[]);
}
