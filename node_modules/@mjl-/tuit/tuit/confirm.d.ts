import * as dom from '../dom';
import * as types from './types';
export declare class Confirm implements types.UI {
    root: HTMLElement;
    confirmed: Promise<void>;
    private button;
    constructor(app: types.Looker, title: string, message: string, action: string);
    focus(): void;
}
export declare const confirm: (app: types.Looker & dom.Rooter, title: string, message: string, action: string) => Promise<boolean>;
