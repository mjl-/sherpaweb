import * as dom from '../dom';
import * as looks from './looks';
import * as types from './types';
export declare class TypeaheadOption implements dom.Rooter {
    value: string;
    root: HTMLElement;
    constructor(app: types.Looker, value: string, typeahead: Typeahead);
}
export declare class Typeahead implements types.UI {
    private app;
    private values;
    private inputLooks;
    private select;
    private focused;
    private blurred;
    root: HTMLElement;
    private popoverBox;
    private optionsAll;
    private optionsFiltered;
    private input;
    private optionsBox;
    private popover;
    constructor(app: types.Looker, value: string, values: string[], inputLooks: looks.Style, inputFocusedLooks: looks.Style, select: (v: string) => void, create: (v: string) => void, focused: () => void, blurred: () => void);
    value(): string;
    focus(): void;
    setValue(v: string): void;
    removeValue(v: string): void;
    addValue(v: string): void;
    filter(ensure: boolean): void;
    selected(o: TypeaheadOption): void;
    isFocused(): boolean;
    checkFocus(): void;
}
