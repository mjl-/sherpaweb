import * as dom from '../dom';
export class Failure {
    constructor(app, message) {
        this.root = dom.div(dom.h1(app.looks.title, 'Error'), dom.div(app.looks.alertDanger, dom.div(app.looks.textWrap, message)));
    }
    focus() {
    }
}
